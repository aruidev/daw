`string r = scan.nextline` => Lo usaremos siempre que necesitemos pedir un dato al usuario.

`n = Integer.parseInt(r)` => Pasamos a entero la respuesta del usuario.

`string a[] = r.split()` => array

`System.out.println`
`System.out.printf`
`System.out.print`


